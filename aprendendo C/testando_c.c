/*
Testando C
*/

#include <stdio.h>
#include <string.h>
#include <stdlib.h>



int main(){
	int quantidade, nan;
	int numeros[200], n = 0, roda_string = 0;
	char lala[200], aux[200];

/*
	for (int i = 0; i < 200; i++)
		lala[i] = ' ';


	printf("teste\n\n\n");
	scanf("%[^\n] ", lala);


	fflush(stdin);
	scanf("%d", &quantidade);
	fflush(stdin);

	for (int i = 0; i < 200; i++)
		aux[i] = ' ';




	for (int i = 0; i < quantidade; i++){
		while (lala[roda_string] != ' '){
			aux[n] = lala[roda_string];
			roda_string++;
			n++;
		}
		numeros[j] = strtol(aux, NULL, 10);

		for (int i = 0; i < 200; i++)
			aux[i] = ' ';

		n=0;
		roda_string++;


	}

	for (int i = 0; i < quantidade; i++)
	{
		printf("%d\n", numeros[i]);
	}
*/
	scanf("%d", &numeros[0]);
	scanf("%d", &numeros[1]);

	printf("%d\n%d\n", numeros[0], numeros[1]);
















}